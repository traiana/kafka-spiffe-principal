pluginManagement {
    repositories {
        maven {
            setUrl("https://arti.tw.ee/artifactory/plugins-release")
        }
        maven {
            setUrl("https://arti.tw.ee/artifactory/libs-release")
        }
        mavenLocal()
    }
}

rootProject.name = 'kafka-spiffe-principal'
